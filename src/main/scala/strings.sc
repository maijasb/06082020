val myName = "Maija" //string is implicit
println(myName)
myName.length
myName(0) //first letter of the name
myName.takeRight(1) //last character of the string. myName(-1) does not work. will not return last char

'A'.toInt
1.toChar
65.toChar
'Ē'.toInt
'Ā'.toInt
65533.toChar
23000.toChar

myName.drop(2)

myName.slice(2,4) // last is not included
myName.toUpperCase

myName.toUpperCase.take(2) //first real chaining example

